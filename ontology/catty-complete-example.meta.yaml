metadata:
  name: "catty-complete-example"
  description: "End-to-end illustrative example tying together logics, morphisms, and lattice structure"
  version: "1.0.0"
  authors:
    - "metavacua"
  created: "2026-01-14"

iri:
  config_key: "catty-complete-example"

rdf:
  format: "JSON-LD"
  context_imports:
    - "catty-categorical-schema"

validation:
  shacl_shapes: []

sparql_examples:
  - query_id: "example-logics"
    description: "Retrieve all logics used in the complete example"
    expected_result_count: "7"
    query: |
      PREFIX catty: <http://localhost:8080/ontology/catty-categorical-schema#>
      SELECT ?logic WHERE {
        ?logic a catty:Logic .
      }
